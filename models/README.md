# Models

This folder is for trained `.h5` model artifacts.

```text
models/
|-- pretrained/   # Final or submitted models
`-- saved/        # Models saved from local notebook runs
```

Model binaries are ignored by Git because they are large. Keep them locally for testing and inference, or publish them separately with Git LFS, a release asset, or cloud storage when needed.
